 //DOM-Document object Model-the entire html bacge is an object called document. it has a lot of elements in it
//Window object represnts the browser window

console.log(document.body)//shows the entire bage body as a document
document.body.style.body="red" //changes background

 //BOM-Browser object Model-this represents the additional objects providded by the browser(host environment) for working wth everything except the document. Alert,confirm,prompt are also part of the BOM